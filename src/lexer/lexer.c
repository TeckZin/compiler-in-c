#include "lexer.h"
#include <stdio.h>

typedef enum {
  TOKEN_INT,
  TOKEN_FLOAT,
  TOKEN_CHAR,

  TOKEN_PLUS,
  TOKEN_MINUS,
  TOKEN_MULTIPLY,
  TOKEN_DIVIDE,
  TOKEN_ASSIGN,

  TOKEN_IF,
  TOKEN_WHILE,
  TOKEN_RETURN,

  TOKEN_EQUAL,
  TOKEN_NOT_EQUAL,

  TOKEN_SEMICOLON,
  TOKEN_LEFT_PAREN,
  TOKEN_RIGHT_PAREN,
  TOKEN_LEFT_BRACE,
  TOKEN_RIGHT_BRACE,

} TokenType;
